Console.WriteLine("Введите число: ");
int N = Convert.ToInt32(Console.ReadLine());
int number = 2;

if (N > 1);
{
    while(number <= N)
    
    {
        Console.WriteLine(number + "");
        number = number + 2;
    }
}
